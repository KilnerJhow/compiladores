#include <stdio.h>
int unum = 1;

float half (float x) {
	return x / 2;
}

int scalar (int a, int b) {
	int array[10];
	array[11] = 0;
	for (int i = 0; i < 10; i++) {
		if (a < b) a -= b;
		array[i] = a;
	}
}

void main(){
    int kkkk[3] = {1, "12", 1.0};
	int kk[1];
	kk[100] = 0;
}